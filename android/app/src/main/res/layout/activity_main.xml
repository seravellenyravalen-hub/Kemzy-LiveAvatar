<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.camera.view.PreviewView
        android:id="@+id/cameraPreview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="#AA000000">

        <TextView
            android:id="@+id/statusText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Camera starting…"
            android:textColor="@android:color/white"
            android:textSize="15sp"
            android:paddingBottom="8dp" />

        <TextView
            android:id="@+id/outputStatusText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Camera output: checking device support…"
            android:textColor="@android:color/white"
            android:textSize="13sp"
            android:paddingBottom="8dp" />

        <Button
            android:id="@+id/backgroundLiveButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Start Background Live" />

        <Button
            android:id="@+id/stopBackgroundLiveButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Stop Background Live"
            android:visibility="gone" />
    </LinearLayout>
</FrameLayout>
